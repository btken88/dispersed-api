{
  "name": "dispersed-api",
  "version": "1.0.0",
  "description": "Firebase Functions API for the Dispersed camping app",
  "private": true,
  "workspaces": [
    "functions"
  ],
  "scripts": {
    "deploy": "cd functions && npm run deploy",
    "serve": "cd functions && npm run serve",
    "test": "cd functions && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/btken88/dispersed-api.git"
  },
  "license": "ISC"
}
